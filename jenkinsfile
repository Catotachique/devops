pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Git checkout is usually automatic in a Pipeline job
                // sh 'git clone <your_repo_url>'  // Only if automatic checkout fails
            }
        }
        stage('Build') {
            when {
                branch 'main' // Or some other condition
            }
            steps {
                // Build steps (e.g., sh 'mvn clean install')
                sh 'echo "Building the project..."' // Example
            }
        }
        stage('Test') {
            when {
                branch 'develop' // Or some other condition
            }
            steps {
                // Test steps (e.g., sh 'mvn test')
                sh 'echo "Running tests..."' // Example
            }
        }
        stage('Deploy') {
            steps {
                // Deployment steps
                sh 'echo "Deploying the project..."' // Example
            }
        }
    }
    post {
        always {
            echo 'Pipeline completed.'
        }
    }
}